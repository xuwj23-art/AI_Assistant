# 项目概览与技术选型说明

## 🎯 项目目标

构建一个**科研文献综述 AI 助手**,帮助研究者快速理解和整理大量学术论文。

**核心价值：**
- 从几百篇论文中自动发现主题
- 生成高质量的主题综述
- 支持与特定主题的智能对话

---

## 🏗️ 系统架构

```
┌─────────────┐
│   用户 UI   │  ← Streamlit 前端
└──────┬──────┘
       │ HTTP
┌──────▼──────────┐
│  FastAPI 后端   │  ← REST API 服务
└──────┬──────────┘
       │
┌──────▼──────────┐
│   核心算法层    │
├─────────────────┤
│ arXiv 数据获取  │  ← Phase 1
│ BERTopic 聚类   │  ← Phase 2
│ LLM 文本生成    │  ← Phase 3
│ RAG 问答系统    │  ← Phase 5
└─────────────────┘
```

---

## 💡 技术选型理由

### 1. Python 3.10+

**为什么选择 Python？**
- AI/ML 生态最完善（PyTorch、TensorFlow、HuggingFace）
- 丰富的 NLP 工具（NLTK、spaCy、Transformers）
- 快速原型开发，适合学术项目

**为什么要 3.10+？**
- 改进的类型提示语法（`list[str]` 而非 `List[str]`）
- 更好的错误信息
- 性能提升（约比 3.8 快 10-15%）

### 2. FastAPI（后端框架）

**为什么选 FastAPI 而非 Flask？**

| 特性 | FastAPI | Flask |
|------|---------|-------|
| 性能 | 极快（基于 Starlette + Pydantic） | 中等 |
| 异步支持 | 原生支持 `async/await` | 需要额外配置 |
| 类型验证 | 自动（Pydantic） | 手动或需插件 |
| API 文档 | 自动生成（Swagger UI） | 需手动编写或用插件 |
| 学习曲线 | 中等 | 简单 |

**FastAPI 的优势：**
- **生产级性能**：与 Node.js、Go 同级别
- **开发效率高**：类型提示 + 自动文档 = 更少代码
- **现代异步**：处理 IO 密集任务（API 调用、数据库查询）更高效
- **行业趋势**：大厂（字节、美团）新项目普遍采用

**代码示例对比：**

```python
# Flask（传统）
from flask import Flask, request, jsonify

app = Flask(__name__)

@app.route('/search', methods=['POST'])
def search():
    data = request.get_json()
    query = data.get('query')  # 可能为 None
    if not query:
        return jsonify({'error': 'Missing query'}), 400
    # ... 处理逻辑
    return jsonify({'results': []})

# FastAPI（现代）
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class SearchRequest(BaseModel):
    query: str
    max_results: int = 100

@app.post('/search')
def search(req: SearchRequest):
    # FastAPI 自动验证，query 不可能为空
    # 自动生成 API 文档
    return {'results': []}
```

### 3. BERTopic（主题建模）

**为什么选 BERTopic 而非传统 LDA？**

| 维度 | LDA | BERTopic |
|------|-----|----------|
| 输入 | Bag-of-Words | Sentence Embeddings |
| 语义理解 | 弱（基于词频） | 强（基于 Transformer） |
| 簇数 | 需预设 | 自动确定 |
| 主题词质量 | 中等 | 高 |
| 短文本 | 效果差 | 效果好 |
| 可视化 | 需额外工具 | 内置丰富可视化 |

**BERTopic 工作流程：**
1. **Sentence-BERT** → 将摘要转成 768 维向量
2. **UMAP** → 降维到 5 维（保留语义结构）
3. **HDBSCAN** → 密度聚类（自动确定簇数）
4. **class-based TF-IDF** → 提取每个簇的代表词

**适用场景：**
- ✅ 学术论文摘要（200-300 词）
- ✅ 新闻标题和正文
- ✅ 社交媒体文本
- ❌ 超长文档（需要先分段）

### 4. LangChain（LLM 应用框架）

**为什么需要 LangChain？**

直接调用 OpenAI API 的痛点：
- 需要手写 prompt 模板拼接逻辑
- 需要手动处理 token 限制
- 需要自己实现 RAG 的检索 + 生成流程
- 难以做 chain（多步骤串联）

**LangChain 提供的抽象：**
- `PromptTemplate`：结构化 prompt 管理
- `LLMChain`：LLM 调用 + 输出解析
- `VectorStoreRetriever`：向量检索封装
- `ConversationalRetrievalChain`：RAG 对话链

**代码示例：**

```python
# 不用 LangChain（手写）
import openai

def summarize_topic(abstracts: list[str]) -> str:
    prompt = f"Summarize these papers:\n\n"
    for abs in abstracts:
        prompt += f"- {abs}\n"
    
    response = openai.ChatCompletion.create(
        model="gpt-3.5-turbo",
        messages=[{"role": "user", "content": prompt}]
    )
    return response.choices[0].message.content

# 用 LangChain（优雅）
from langchain.prompts import PromptTemplate
from langchain.chains import LLMChain
from langchain_openai import ChatOpenAI

template = """
Summarize the following research papers:

{abstracts}

Provide a concise synthesis of common themes and methods.
"""

prompt = PromptTemplate(template=template, input_variables=["abstracts"])
llm = ChatOpenAI(model="gpt-3.5-turbo")
chain = LLMChain(llm=llm, prompt=prompt)

result = chain.run(abstracts="\n".join(abstracts))
```

### 5. ChromaDB（向量数据库）

**为什么需要向量数据库？**

RAG（检索增强生成）的核心：给定一个问题，找到最相关的文档。

**传统检索（关键词匹配）：**
- 问题："PEFT 的主要方法是什么？"
- 检索：搜索包含 "PEFT" 或 "parameter-efficient" 的论文
- 问题：无法处理同义词、语义相似

**向量检索（语义匹配）：**
- 问题 → 向量：`[0.12, -0.34, ..., 0.56]`
- 计算：与所有论文向量的余弦相似度
- 返回：Top-5 最相似的论文（即使不包含关键词）

**ChromaDB vs 其他方案：**

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **ChromaDB** | 轻量、易用、本地运行 | 性能一般 | 小规模原型（< 100 万条） |
| Pinecone | 高性能、云服务 | 需付费、依赖网络 | 生产环境 |
| Milvus | 高性能、开源 | 部署复杂 | 大规模生产 |
| FAISS | 极快 | 仅向量检索，无元数据管理 | 纯向量搜索 |

**对你的项目：** ChromaDB 完全够用，因为：
- 数据量小（100-1000 篇论文）
- 本地运行，无需云服务
- API 简洁，易于学习

### 6. Streamlit（前端框架）

**为什么选 Streamlit 而非 React？**

| 维度 | Streamlit | React |
|------|-----------|-------|
| 开发语言 | Python | JavaScript |
| 学习曲线 | 极低 | 中高 |
| 开发速度 | 极快（几十行代码） | 中等 |
| UI 灵活性 | 中等 | 极高 |
| 适用场景 | 数据应用、内部工具 | 复杂 Web 应用 |

**Streamlit 的优势（对你的项目）：**
- ✅ **纯 Python**：不需要学 JS/HTML/CSS
- ✅ **快速原型**：专注业务逻辑，不纠结 UI 细节
- ✅ **内置组件**：图表、表格、侧边栏开箱即用
- ✅ **学术友好**：很多研究者用它做 demo

**代码示例：**

```python
import streamlit as st
import pandas as pd

st.title("论文检索")

query = st.text_input("输入关键词")
if st.button("搜索"):
    # 调用后端 API
    papers = fetch_papers(query)
    df = pd.DataFrame(papers)
    st.dataframe(df)
```

10 行代码 = 一个可用的 UI！

---

## 📊 数据流示意图

```
1. 用户输入关键词 "large language models"
   ↓
2. arXiv API 返回 JSON 数据
   {
     "id": "...",
     "title": "...",
     "abstract": "...",
     "authors": ["..."],
     ...
   }
   ↓
3. Pydantic 验证 → ArxivPaper 对象
   ↓
4. 列表 → Pandas DataFrame
   ↓
5. DataFrame["abstract"] → Sentence Embeddings
   ["LLMs...", "Transformers...", ...] 
   → [[0.12, -0.34, ...], [0.45, 0.23, ...], ...]
   ↓
6. BERTopic 聚类
   → Topic 0: "Model Training" (30 papers)
   → Topic 1: "Inference Optimization" (25 papers)
   → Topic 2: "Evaluation" (20 papers)
   ↓
7. LLM 生成综述
   "Topic 0 focuses on training methodologies..."
   ↓
8. Streamlit 展示
   [可视化] [论文列表] [综述文本]
```

---

## 🎓 学习路径：从简到难

### 阶段 1：Python 工程化基础
- 虚拟环境（venv）
- 依赖管理（requirements.txt）
- 模块化设计（`__init__.py`、相对导入）
- 类型提示（Type Hints）

### 阶段 2：NLP 核心概念
- 词向量 vs 句向量
- Transformer 架构（直观理解）
- 句向量模型（Sentence-BERT）
- 主题建模（BERTopic）

### 阶段 3：后端开发
- REST API 设计原则
- FastAPI + Pydantic
- 异步编程（`async/await`）
- API 文档（Swagger）

### 阶段 4：前端 + 可视化
- Streamlit 组件
- Pandas 数据处理
- Plotly 交互式图表

### 阶段 5：生成式 AI 应用
- LLM API 调用（OpenAI）
- Prompt Engineering
- RAG 架构
- 向量数据库（ChromaDB）

---

## 🚀 与大厂技术栈对齐

### 字节跳动/飞书 AI 应用工程师

**他们用什么：**
- Python + FastAPI（服务端）
- LangChain / 自研 LLM 框架
- 向量数据库（内部方案类似 Milvus）
- React / Vue（前端）

**你学到什么：**
- ✅ Python 工程化实践
- ✅ FastAPI 后端开发
- ✅ LLM 应用开发（LangChain）
- ✅ 向量检索与 RAG
- ⚠️ 前端：你用 Streamlit，他们用 React（但核心逻辑相通）

**如何弥补差距：**
- 项目完成后，可以考虑用 React 重写前端（Phase 6 扩展）
- 或者，在简历中强调"快速原型能力"和"全栈思维"

---

## 📈 预期成果

完成本项目后，你将拥有：

### 技术能力
- ✅ 独立开发 AI 应用的能力
- ✅ 理解 NLP 核心技术（embedding、聚类、生成）
- ✅ 熟悉现代 Python 工程实践
- ✅ 掌握 LLM 应用开发流程

### 可展示的作品
- 📊 一个可运行的 Web 应用（有 UI、有后端）
- 📄 清晰的代码仓库（模块化、有文档、有注释）
- 🎨 可视化 demo（主题分布图、论文聚类图）
- 💬 RAG 对话功能（展示技术深度）

### 简历亮点
- "基于 BERTopic 的学术论文主题发现系统"
- "使用 LangChain 和 OpenAI API 实现 RAG 问答"
- "FastAPI + Streamlit 全栈开发实践"
- "处理 100+ 篇论文的自动化分析"

---

## 🎯 下一步行动

1. ✅ **立即开始 Phase 1**：运行 `fetch_and_save_arxiv.py`
2. 📖 **阅读核心代码**：理解 `src/core/arxiv/` 的设计
3. 📊 **数据探索**：用 Jupyter 分析论文分布
4. 📝 **记录问题**：遇到的坑和解决方案（面试会问）

**准备好了吗？** 打开 `QUICKSTART.md` 开始实战！

